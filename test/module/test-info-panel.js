/*
let infopanel = require("info-panel");
let panel = require('panel');

exports["test info-panel opens"] = function(assert) {
    let testPanel = infopanel.createPanel(120, 120);
    assert.waitUntilDone();
    infopanel.showPanel(testPanel, "test");
    testPanel.on("show", function() {
        assert.pass("panel is open");
        assert.done();
    });
};
*/
